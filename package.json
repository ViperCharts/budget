{
  "name": "budget",
  "private": true,
  "version": "0.0.1",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --cwd packages/app dev",
    "dev:local": "concurrently --kill-others-on-fail --names \"emulators,app\" --prefix-colors \"yellow,cyan\" \"firebase emulators:start\" \"VITE_USE_EMULATOR=true bun run --cwd packages/app dev\"",
    "emulators": "firebase emulators:start",
    "build": "bun run --cwd packages/app build",
    "preview": "bun run --cwd packages/app preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
